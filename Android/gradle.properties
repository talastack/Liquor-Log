org.gradle.jvmargs=-Xmx2g
org.gradle.caching=true
kotlin.code.style=official

# Required the moment anything depends on AndroidX, which Compose and the
# SQLDelight Android driver both do. Without it the build refuses rather than
# producing something that fails at runtime.
android.useAndroidX=true
